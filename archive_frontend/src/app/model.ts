export interface UploadGroup {
    name: string
    title: string
    comments: string
    archive: string
    
}

export interface Bundle {
    bundleId: string
    date: string
    title: string
    name: string
    comments: string
    url: string[]
}